import TileTable from "../components/TileTable";
import { getTiles } from "../data/db";
import { Callout } from "nextra/components";

export async function getStaticProps() {
  const data = await getTiles();
  return {
    props: {
      data,
    },
  };
}

# Tile Table

<Callout type="warning" emoji="⚠️">
  This Table component is still a work in progress. Visuals and features may change over time. Mobile/small screen layout is likely to have issues. Bug reports and contributions are welcome!
</Callout>
<TileTable data={props.data} />
